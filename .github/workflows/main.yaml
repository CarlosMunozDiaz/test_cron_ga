name: test_cron_ga

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  this-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
      - run : npm install

      - name: primer-paso
        run: echo "Estamos realizando primer paso"

      - name: segundo-paso
        run: node index.js